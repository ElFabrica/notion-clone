import { AuthConfig } from "convex/server";

export default {
  providers: [
    {
      domain: "obliging-beetle-53.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
} satisfies AuthConfig;
